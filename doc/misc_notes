
Bluetooth issues:
	Still having problems connecting to BT in Linux.
	Some partial remedies:
		sudo apt-get install bluetooth
		sudo usermod -a -G dialout <user>
		sudo systemctl disable ModemManager
